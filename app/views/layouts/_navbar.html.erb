<nav class="navbar navbar-dark bg-dark">
  <ul class="navbar-nav mr-auto">
    <a class="navbar-brand" href="/">Cooked Nuggets</a>
  </ul>
  <ul class="navbar-nav" style="flex-direction: row">
    <% if logged_in? %>
      <li class="nav-item mx-2"><div class="username"><%= @user.username %></div></li>
      <li class="nav-item mx-2"><%= link_to 'Log Out', logout_path, method: :delete, class: "btn btn-outline-light" %></li>
    <% else %>
      <li class="nav-item mx-2"><%= link_to 'Log In', login_path, class: "btn btn-outline-light" %></li>
      <li class="nav-item mx-2"><%= link_to 'Sign Up', signup_path, class: "btn btn-outline-light" %></li>
    <% end %>
  </ul>
</nav>
